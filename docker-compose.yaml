version: "3"

services:
  redis:
    image: "redis"
    container_name: "redis"
    ports:
      - "6379:6379"
    restart: always
  db:
    image: "mongo"
    container_name: "mongo"
    ports:
      - "27017:27017"
    restart: always